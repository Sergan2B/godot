Project-scoped Godot tools use the package launcher bound to the trusted project. Call godot_get_connection_status or read godot://connection/status before assuming editor/runtime availability. Offline data is saved-project cache only: never claim live editor/runtime state; writes are unavailable. Before any write, prepare and inspect the immutable preview. Codex sandbox approval and MCP action-only form approval are independent and both required; neither substitutes for the other. Follow connection remediation without treating status as evidence for an editor fact. Read godot://project/summary for saved-project questions, godot://editor/summary for current editor questions, godot://runtime/summary for the local game lifecycle, and godot://scene/{scene_id}/summary for indexed scene questions. Runtime controls affect only the ephemeral local game and never write scenes or scripts; pass the returned runtime_session_id and expected sequence to guarded operations. For an editor write, read a current snapshot, prepare the smallest change, obtain both approvals, apply the unchanged transaction once, then read back affected facts and inspect validation before targeted godot_undo_transaction when requested. Undo is eligible only while the exact native history action remains current. Never infer approval or replay apply after an uncertain response; query godot_get_transaction_status and follow remediation instead. Never reuse project, session, transaction, report, or evidence coordinates across project roots. For setup or doctor remediation, use only the package-owned absolute operations launcher paired with this server's configured absolute `godot-codex-mcp` command: the `godot-codex` sibling in the same `bin` directory. Use it with `setup` or `doctor --project-root <path> --json`; never substitute a default-global launcher, PATH basename, or checkout binary. Use focused editor/runtime tools, cite evidence IDs, distinguish disk/editor/runtime state, and keep bounded projections.
